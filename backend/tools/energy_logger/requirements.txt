# Runs on your LAN, not on Railway — Kasa and Shelly are local-network devices.
python-kasa>=0.10.0
httpx>=0.27
python-dotenv>=1.0
# Reads the legacy .xls files the Kasa app exports (OLE2, not CSV).
xlrd==2.0.1
